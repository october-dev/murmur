[package]
name = "murmur-protocol"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Language-neutral Murmur voice protocol models for Rust."
repository = "https://github.com/october-dev/murmur"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
